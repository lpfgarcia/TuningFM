[project]
name = "tuningfm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lpfgarcia", email = "lpfgarcia@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
tuningfm = "tuningfm:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
